function Server() {
  var framerate = 60;
  var lastUpdateTime = 0;
  var que = {};
}

Server.prototype.start = function () {
  var that = this;
  var serverStart = setInterval(
    function() { 
      that.update(); 
    }, 1000/framerate);
}

Server.prototype.addToQue = function(item) {
  que.push(item);
}

Server.prototype.update = function() {
  // Run game updates here
  // for (item in que) {
    
  // }
}

module.exports = Server;